@model IEnumerable<OnlineHouseRenting.Models.tbl_category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HOUSE_CATEGORY)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HOUSE_CATEGORY)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HOUSE_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HOUSE_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HOUSE_ID })
        </td>
    </tr>
}

</table>
